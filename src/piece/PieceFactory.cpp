//
// Created by jw on 26.01.19.
//

#include "PieceFactory.h"



Piece PieceFactory::createPawn(Color) {
    return Piece(white, nullptr, <#initializer#>);
}

Piece PieceFactory::createBishop(Color) {
    return Piece(white, nullptr, <#initializer#>);
}

Piece PieceFactory::createKnight(Color) {
    return Piece(white, nullptr, <#initializer#>);
}

Piece PieceFactory::createRook(Color) {
    return Piece(white, nullptr, <#initializer#>);
}

Piece PieceFactory::createQueen(Color) {
    return Piece(white, nullptr, <#initializer#>);
}

Piece PieceFactory::createKing(Color) {
    return Piece(white, nullptr, <#initializer#>);
}
